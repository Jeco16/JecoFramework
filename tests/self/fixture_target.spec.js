import { test } from '../fixtures/fixtures.js';

test('SELF_99 - fixture write target', async ({ testData }) => {
  // Use testData so fixture executes and writes metadata on teardown
  void testData;
});
